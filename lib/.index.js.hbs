/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const create = require('./create')
const AKoa = require('./akoa')

let lib = create.bind(this)

Object.assign(lib, create, {
  create,
  AKoa
})

module.exports = lib
